생성된 용어를 기반으로 데이터베이스를 설계할거야. 데이터베이스, 엔터티, 속성, 테이블, 컬럼 각각을 기존 단어, 도메인, 용어와 같이 json 파일로 관리하고 기능 또한 유사하게 CRUD와 파일 다운로드를 구현하는게 설계 목표야. 프로젝트 구조는 @docs를 참고하고, 업로드 및 다운로드 파일 형태는 @references를 참고해.

[프로퍼티]

- 속성 끝에 문자를 붙이는데 이는 해당 속성의 Nullable 여부임(T:참, F:거짓)
- 예시 : 논리DB명T / 수퍼타입엔터티명F

[속성]

- 데이터베이스 : 기관명F, 부서명F, 적용업무F, 관련법령F, 논리DB명T, 물리DB명T, 구축일자F, DB설명T, DBMS정보T, 운영체제정보F, 수집제외사유F
- 엔터티 : 논리DB명T, 스키마명T, 엔터티명T, 엔터티설명T, 주식별자T, 수퍼타입엔터티명F, 테이블한글명T
- 속성 : 스키마명T, 엔터티명T, 속성명T, 속성유형T, 필수입력여부F, 식별자여부T, 참조엔터티명F, 참조속성명T, 속성설명T
- 테이블 : 물리DB명T, 테이블소유자T, 주제영역T, 스키마명T, 테이블영문명T, 테이블한글명T, 테이블유형T, 관련엔터티명T, 테이블설명T, 업무분류체계F, 보존기간T, 테이블볼륨F, 발생주기T, 공개/비공개여부T, 비공개사유F, 개방데이터목록F
- 컬럼 : 사업범위여부T, 주제영역T, 스키마명T, 테이블영문명T, 컬럼영문명T, 컬럼한글명T, 컬럼설명T, 연관엔터티명T, 자료타입T, 자료길이F, 자료소수점길이F, 자료형식F, NOTNULL여부T, PK정보F, FK정보, 인덱스명F, 인덱스순번F, AK정보F, 제약조건F, 개인정보여부T, 암호화여부T, 공개/비공개여부

---

@prompts/create_database_feature.md 를 참고해서 개발을 진행 중이야. @docs/FRONTEND_UI_UX_GUIDE.md 를 참고해서 UI를 구현해

---

@prompts/create_database_feature.md 를 기반으로 데이터베이스, 엔터티, 속성, 테이블, 컬럼 메뉴와 기능을 생성했는데 오류가 발생해. 단계적으로 관련 오류를 수정시킬거야.

1. 파일 관리 팝업에 시스템 파일 표시 기능이 없어 추가 필요해.
2. 파일 업로드에서 업로드 모드 중 병합과 덮어쓰기는 삭제됐어.
3. 파일 업로드 기능이 정상 동작하지 않아. 현재는 검증이 구체화되지 않았으니 검증 교체 모드와 단순 교체 모드를 동일하게 구현하고, 추후 검증 교체 모드에만 검증을 붙이는 방향으로 갈거야.
4. 앞선 오류들이 다시 일어나지 않도록 @docs 폴더에 관련 문서들을 수정해

---

앞으로 일어난 작업들에 대해서도 일관적으로 @docs 폴더 내 관련 문서들을 수정해

1. 파일 관리의 시스템 파일 표시 여부가 바깥 목록 페이지에도 영향을 줘야해
2. 파일 관리의 이름변경과 삭제 아이콘이 존재해.@src/lib/components/TermFileManager.svelte 를 참고해
3. 파일 관리에서 새 파일 생성 input에 enter key event 추가해
4. 정의서 추가/수정 팝업이 grid로 구성되어 있는 것 같은데 1행에는 1열만 들어가게 변경해. @src/lib/components/DomainEditor.svelte 를 참고해

---

1. 파일이 삭제되거나 시스템 파일 표시가 꺼지면서 다른 파일로 변경되면 현재 파일 정보도 변경된 것에 맞춰서 바뀌어야 해.
2. 목록에 필터와 정렬이 제대로 구현되지 않은 것 같아. @src/lib/components/VocabularyTable.svelte 을 참고해서 수정해.
3. 시스템 파일 표시는 @static/global/settings.json 에 저장시켜서 해당값 사용하고 있어. @src/lib/components/VocabularyFileManager.svelte 참고해서 수정해.
4. 데이터베이스 첨부 파일 업로드 시 컬럼 매핑이 제대로 안 되네. 한 칸씩 밀리고 있는데 혹시 첫 번째 열은 index로 취급해서 제거 중인가? 현재 업로드 중인 파일들은 index 없이 A열부터 바로 데이터야. references의 파일들이 샘플이니 참고해

---

1. 정렬 및 필터 UI가 기존과 달라. @src/lib/components/VocabularyTable.svelte 확인해서 다시 수정해
2. @static/global/settings.json 의 show${메뉴별}SystemFiles로 시스템 파일 표시 여부를 사용하고 있는데 이 부분 적용이 제대로 안 됐어
3. 목록의 건수가 제대로 표출되지 않아. UI도 다시 한 번 기존 테이블 참고해서 수정해

---

[지시사항]

- 앞으로 일어난 작업들에 대해서 일관적으로 폴더 내 관련 문서들을 수정해
- 기능상의 관련 여파를 항상 고려해서 관련된 작업도 무조건 같이 작업해
- 현재 기존에 만들어진 단어집, 도메인, 용어를 기반으로 DB, 엔터티, 속성, 테이블, 컬럼을 추가하는 작업을 진행 중임

[수정사항]

1. 파일 관리의 시스템 파일 표시가 @static/global/settings.json 에 적용되어야 하는데 이 부분이 적용 안됨. 정확한 기능은 시스템 파일 표시 여부를 저장해 새로고침 되거나 서비스가 다시 시작됐을 때도 유지되는 것이 목표임
2. 각각의 목록에서 검색 결과에 전체 ${메뉴명} n건 부분에 건수가 0으로만 표출됨. UI도 기존의 테이블들은 ${메뉴명} 목록 총 n건 항목 과 같이 표시되는데 이 부분도 적용되지 않음.

---

[수정사항]

1. 정의서 등록/수정 팝업의 scroll 적용 위치가 기존의 등록/수정 팝업과 달라 확인해서 수정해
2. 통합검색 대상 항목을 정의하고, 고급 검색을 수정할거야.

- DB : 기관명, 논리DB명, 물리DB명
- 엔터티 : 스키마명, 엔터티명, 주식별자, 수퍼타입엔터티명, 테이블한글명
- 속성 : 스키마명, 엔터티명, 속성명
- 테이블 : 물리DB명, 스키마명, 테이블영문명, 테이블한글명, 테이블유형, 주제영역
- 컬럼 : 스키마명, 테이블영문명, 컬럼영문명, 컬럼한글명, 자료타입
